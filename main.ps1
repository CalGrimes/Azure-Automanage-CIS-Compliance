# Open an Azure Cloud Shell terminal and run the following commands:

# 1. Login to Azure
az login

# 2. Run terraform init
terraform init

# 3. Create a terraform plan
terraform plan -out main.tfplan